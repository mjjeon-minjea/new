# 텔레그램 능동형 프로액티브 모니터링 & AI 합동 언론 데스크 파이프라인 구현 계획서

**작성일**: 2026-05-23
**작성자**: Claude (R10 계획서 결함 교정 및 누락 기능 추가)
**감사 대상 코드**:
- `agents/chief_agent.py` (248줄 전문 독해)
- `telegram_bot.py` (782줄 전문 독해)
- `agents/shared/ollama_client.py` (81줄 전문 독해)
- `agents/shared/config.py` (55줄 전문 독해)
- `agents/shared/protocols.py` (AgentResult dataclass 확인)
**수정번호**: R11

---

## 0. 코드 사전 감사 결과 (Step 0 — R10 결함 교정 근거)

R11을 작성하기 전, 실물 코드를 직접 정독하여 R10 계획서의 결함을 확인하였습니다.

### R10의 치명적 오류 3건

**① Before 코드가 허구였다**
R10의 Before 코드는 `run_multi_agent_debate(raw_reports, ...)` 호출을 참조하고 있습니다.
`raw_reports` 변수는 실제 `run_relay()` 어디에도 존재하지 않습니다. R9 가공물을 실수로 포함시킨 것으로, 실물 Before 코드는 아래와 같습니다 (`chief_agent.py` 64~82줄):

```python
# 실제 현행 코드 (Before)
reporters = [
    KoreaReporter(),
    GlobalReporter(),
    BitcoinReporter()
]

for reporter in reporters:
    rep_res = reporter.collect(financial_data)
    results.append(rep_res)
    print(f"[*] [{reporter.agent_name}] 처리 성공 여부: {rep_res.success} | 수집 기사 수: {rep_res.collected_count}개")

# ---------------------------------------------
# [4선: Wiki 관리자 - 지식 융합 및 누적 컴파운딩]
# ---------------------------------------------
wiki_agent = WikiManager()
wiki_res = wiki_agent.run_compounding(financial_data)
results.append(wiki_res)
```

**② `wiki_agent.run_compounding()` 시그니처 오인**
R10 After 코드: `wiki_agent.run_compounding(financial_data, final_editorial_result)` — 인자 2개
실물 시그니처: `WikiManager.run_compounding(financial_data)` — 인자 1개
이 코드를 그대로 실행하면 즉시 `TypeError`가 발생합니다.

**③ `_build_briefing_report`의 하드코딩 인덱스 무시**
`_build_briefing_report`(175줄)는 `results[0]`~`results[4]`를 하드코딩으로 참조합니다.
R10 계획대로 `results`에 editorial_result를 삽입하면 `results[4]`가 wiki가 아닌 editorial로 교체되어 `IndexError`가 발생합니다.

**④ 사용자의 최우선 요청인 '능동형 프로액티브 모니터링'이 R10에 없었다**
R10 전문 어디에도 1시간 주기 임계값 감시 및 Telegram 능동 알림 기능이 없습니다.

### 추가 발견: 기존 버그 (`telegram_bot.py`)

`main()` 함수(740줄)에 `init_chat_db()` 호출이 없습니다. `--send-briefing` 진입 분기(779줄)도 마찬가지입니다. 현재 상태에서 봇을 기동하면 첫 번째 `log_chat_message()` 호출 시 `OperationalError: no such table: telegram_chat_logs`가 발생합니다. R11에서 함께 수정합니다.

### Ollama 타임아웃 현황

`call_ollama()`는 `timeout=90`이 하드코딩되어 있습니다 (`ollama_client.py` 48줄). 레드팀 파이프라인은 Ollama를 최대 5회 순차 호출(레드팀 1회 + 기자 수정 3회 + 편집장 1회)하므로 최악의 경우 450초가 소요됩니다. 이 점은 Known Unknowns에 기재합니다.

---

## 1. 구현 범위 요약

| 기능 | 파일 | 변경 종류 |
|---|---|---|
| 능동형 프로액티브 모니터링 루프 | `telegram_bot.py` | 신규 함수 + `main()` 등록 |
| 프로액티브 콜백 핸들러 | `telegram_bot.py` | `button_callback_handler` 분기 추가 |
| 기존 버그 수정 (`init_chat_db` 누락) | `telegram_bot.py` | `main()` 수정 |
| AI 합동 언론 데스크 파이프라인 | `agents/editorial_desk.py` | 신규 파일 |
| 리포터-편집장 연결 오케스트레이션 | `agents/chief_agent.py` | `run_relay()` 수정 |

---

## 2. 신규 파일: `agents/editorial_desk.py`

### 설계 원칙

- 리포터의 취재 원문은 **메모리가 아닌 파일**에서 읽습니다. `AgentResult.files_created`가 이 세션에서 리포터가 실제로 쓴 파일 경로 목록을 담고 있습니다. 해당 경로를 직접 읽습니다.
- 레드팀 → 수정 → 편집장의 3단계 루프 전체를 `call_ollama()` 동기 호출로 처리합니다. 비동기 오케스트레이션은 `chief_agent.py`의 `asyncio.to_thread(agent.run_relay)` 래퍼가 이미 담당하므로 `editorial_desk.py` 내부는 순수 동기 코드입니다.
- `results` 리스트에 `editorial_result`를 **추가하지 않습니다**. `_build_briefing_report()`의 하드코딩 인덱스 파괴를 막기 위해 편집장 결과물은 세션 디렉토리에 별도 저장합니다.

### 전체 코드

```python
# agents/editorial_desk.py
import time
from pathlib import Path
from typing import List, Optional
from agents.shared.protocols import AgentResult
from agents.shared.ollama_client import call_ollama

# ─── 페르소나 시스템 프롬프트 ───────────────────────────────────────────────────

_REDTEAM_SYSTEM = (
    "당신은 20년 경력의 냉혹한 팩트체커 레드팀 논설위원입니다. "
    "제출된 원고에서 논리적 인과관계 비약, 수치 불일치, 거시경제 내적 모순을 "
    "기자명별로 명확하게 구분하여 지적합니다. 칭찬은 일절 없으며, 구체적 수치와 "
    "논리로 근거를 제시합니다. 한국어로 답변합니다."
)

_CORRECTION_SYSTEM = (
    "당신은 레드팀의 지적을 전달받은 전문 경제 기자입니다. "
    "지적된 논리 오류와 수치 모순을 수정하고, 팩트를 보강하여 원고를 개선합니다. "
    "레드팀이 지적하지 않은 부분은 원문을 최대한 유지합니다. 한국어로 답변합니다."
)

_EDITOR_SYSTEM = (
    "당신은 최고급 금융경제 종합지의 편집장입니다. "
    "국내경제, 글로벌매크로, 비트코인 3개 분야의 수정 원고를 "
    "서론-본론(분야 간 인과 결합)-전망 구조의 하나의 유기적인 종합 사설로 "
    "편찬합니다. 각 분야가 서로 독립적으로 나열되지 않고 인과관계로 연결되도록 "
    "윤문합니다. 한국어로 작성합니다."
)

# ─── 헬퍼 ───────────────────────────────────────────────────────────────────────

def _read_reporter_files(rep_res: AgentResult, max_files: int = 3, chars_per_file: int = 2500) -> str:
    """
    AgentResult.files_created에서 최신 N개 파일을 읽어 합본 텍스트로 반환합니다.
    파일당 글자 수를 제한하여 Ollama 프롬프트가 과도하게 길어지는 것을 방지합니다.
    files_created가 비어 있으면 빈 문자열을 반환합니다 (리포터 수집 실패 케이스).
    """
    contents = []
    # 최신 파일 우선으로 취하기 위해 역순으로 슬라이싱
    target_files = rep_res.files_created[-max_files:]
    for f_path_str in target_files:
        try:
            text = Path(f_path_str).read_text(encoding="utf-8")
            contents.append(f"--- {Path(f_path_str).name} ---\n{text[:chars_per_file]}")
        except Exception as e:
            print(f"[!] [EditorialDesk] 리포터 파일 읽기 실패: {f_path_str} — {e}")
    return "\n\n".join(contents)


# ─── 메인 진입점 ────────────────────────────────────────────────────────────────

def run_editorial_board(
    reporter_results: List[AgentResult],
    financial_data: dict,
    session_dir: Path,
) -> Optional[AgentResult]:
    """
    [AI 합동 언론 데스크 3단계 파이프라인]
    1단계 — 레드팀 교차 감사: 3대 기자 원고 합본을 레드팀 페르소나로 비판
    2단계 — 기자단 수정:      각 기자 원고 + 레드팀 지적 → 2차 수정본 생성
    3단계 — 편집장 종합:      3개 수정본 → 하나의 종합 사설 칼럼

    반환: AgentResult (성공 시 payload["final_column"]에 최종 사설 저장)
          Ollama 완전 장애 시 None 반환 (chief_agent.py에서 None 체크 필수)
    """
    start_time = time.time()
    minutes_lines = [
        f"# AI 합동 언론 데스크 편집 회의록\n",
        f"**세션**: `{session_dir.name}`\n\n---\n\n",
    ]

    # ── 1단계: 각 리포터의 실제 파일 읽기 ──────────────────────────────────────
    print("[*] [EditorialDesk] 리포터 원고 파일 수집 중...")
    raw_texts: dict[str, str] = {}
    for rep_res in reporter_results:
        content = _read_reporter_files(rep_res)
        raw_texts[rep_res.agent_name] = content
        status = f"{len(content)}자 수집" if content else "원고 없음 (리포터 실패)"
        print(f"[*] [EditorialDesk]   └ {rep_res.agent_name}: {status}")

    # 모든 리포터가 실패한 경우 조기 종료
    if not any(raw_texts.values()):
        print("[!] [EditorialDesk] 모든 리포터의 파일이 비어 있어 편집 데스크 세션을 중단합니다.")
        return None

    # ── 2단계: 레드팀 교차 감사 ─────────────────────────────────────────────────
    print("[*] [EditorialDesk] 레드팀 교차 감사 개시...")
    all_raw_combined = "\n\n".join(
        [f"## [{name} 1차 원고]\n{text}" for name, text in raw_texts.items() if text]
    )
    redteam_prompt = (
        f"아래는 금융경제 3대 분야 기자들이 제출한 1차 취재 원고입니다.\n\n"
        f"{all_raw_combined}\n\n"
        "각 기자별로 다음 항목을 반드시 짚어 감사 지침을 발부하십시오:\n"
        "① 논리적 인과관계 비약 (예: A가 원인이라고 주장하나 B가 원인인 경우)\n"
        "② 수치 불일치 (예: 금리 인하 기대감과 금리 인상론의 동시 서술)\n"
        "③ 거시경제 내적 모순 (예: 달러 강세와 비트코인 상승을 동시에 호재로 기술)\n"
        "기자명을 명시하여 각각 구분하십시오."
    )

    redteam_critique = call_ollama(redteam_prompt, _REDTEAM_SYSTEM)

    if not redteam_critique:
        print("[!] [EditorialDesk] 레드팀 Ollama 호출 실패. 편집 데스크를 중단합니다.")
        return None

    minutes_lines.append(f"## 1단계: 레드팀 감사 지침\n\n{redteam_critique}\n\n---\n\n")
    print("[+] [EditorialDesk] 레드팀 감사 지침 발부 완료.")

    # ── 3단계: 기자단 수정 루프 ─────────────────────────────────────────────────
    corrected_texts: dict[str, str] = {}
    for rep_res in reporter_results:
        original = raw_texts.get(rep_res.agent_name, "")
        if not original:
            corrected_texts[rep_res.agent_name] = ""
            continue

        print(f"[*] [EditorialDesk] {rep_res.agent_name} 수정 원고 생성 중...")
        correction_prompt = (
            f"[레드팀 감사 지침 전문]\n{redteam_critique}\n\n"
            f"[{rep_res.agent_name} 1차 원고]\n{original}\n\n"
            "위 레드팀의 지적 사항 중 본인의 원고에 해당하는 부분을 수정하십시오. "
            "수치적 근거를 보완하고, 논리적 일관성을 회복하여 2차 수정 원고를 제출하십시오."
        )
        corrected = call_ollama(correction_prompt, _CORRECTION_SYSTEM)
        corrected_texts[rep_res.agent_name] = corrected or original  # Ollama 실패 시 원본 유지
        print(f"[+] [EditorialDesk]   └ {rep_res.agent_name} 수정 완료.")
        minutes_lines.append(
            f"## 2단계: {rep_res.agent_name} 수정 원고\n\n{corrected_texts[rep_res.agent_name]}\n\n---\n\n"
        )

    # ── 4단계: 편집장 최종 종합 사설 ────────────────────────────────────────────
    print("[*] [EditorialDesk] 편집장 최종 종합 사설 작성 중...")
    all_corrected = "\n\n".join(
        [f"## [{name} 최종 수정본]\n{text}" for name, text in corrected_texts.items() if text]
    )
    editor_prompt = (
        f"아래는 레드팀의 감사를 거쳐 수정된 3개 분야의 최종 원고입니다.\n\n"
        f"{all_corrected}\n\n"
        "서론-본론(분야 간 유기적 인과 결합)-전망 구조로 "
        "하나의 통합된 금융경제 종합 사설을 편찬하십시오. "
        "각 분야를 단순 나열하지 말고 상호 인과관계로 연결하십시오."
    )

    final_column = call_ollama(editor_prompt, _EDITOR_SYSTEM)

    if final_column:
        minutes_lines.append(f"## 3단계: 편집장 최종 종합 사설\n\n{final_column}\n")
        print("[+] [EditorialDesk] 편집장 최종 종합 사설 완성.")
    else:
        print("[!] [EditorialDesk] 편집장 Ollama 호출 실패. 수정 원고 합본으로 대체합니다.")
        final_column = all_corrected  # 폴백: 수정 원고 합본 반환

    # ── 5단계: 회의록 저장 ──────────────────────────────────────────────────────
    minutes_content = "".join(minutes_lines)
    files_created = []

    try:
        minutes_path = session_dir / "editorial_minutes.md"
        minutes_path.write_text(minutes_content, encoding="utf-8")
        files_created.append(str(minutes_path))
        print(f"[+] [EditorialDesk] 편집 회의록 저장 완료: {minutes_path.name}")
    except Exception as e:
        print(f"[!] [EditorialDesk] 회의록 저장 실패: {e}")

    elapsed = time.time() - start_time
    print(f"[+] [EditorialDesk] 전체 편집 데스크 세션 완료 (소요: {elapsed:.1f}초).")

    return AgentResult(
        agent_name="EditorialDesk",
        success=True,
        collected_count=len([t for t in corrected_texts.values() if t]),
        files_created=files_created,
        elapsed_seconds=elapsed,
        payload={"final_column": final_column},
    )
```

---

## 3. 변경: `agents/chief_agent.py` — `run_relay()`

### [Before] 실제 현행 코드 (64~82줄)

```python
        reporters = [
            KoreaReporter(),
            GlobalReporter(),
            BitcoinReporter()
        ]
        
        for reporter in reporters:
            rep_res = reporter.collect(financial_data)
            results.append(rep_res)
            print(f"[*] [{reporter.agent_name}] 처리 성공 여부: {rep_res.success} | 수집 기사 수: {rep_res.collected_count}개")
            
        # ---------------------------------------------
        # [4선: Wiki 관리자 - 지식 융합 및 누적 컴파운딩]
        # ---------------------------------------------
        wiki_agent = WikiManager()
        wiki_res = wiki_agent.run_compounding(financial_data)
        results.append(wiki_res)
        print(f"[*] [4선 Wiki관리자] 위키 누적 합성 성공 여부: {wiki_res.success} | 갱신된 파일 수: {wiki_res.collected_count}개")
```

### [After] 수정 코드

```python
        reporters = [
            KoreaReporter(),
            GlobalReporter(),
            BitcoinReporter()
        ]

        reporter_results: List[AgentResult] = []  # 편집 데스크 전달용 별도 보관
        for reporter in reporters:
            rep_res = reporter.collect(financial_data)
            results.append(rep_res)
            reporter_results.append(rep_res)
            print(f"[*] [{reporter.agent_name}] 처리 성공 여부: {rep_res.success} | 수집 기사 수: {rep_res.collected_count}개")

        # ---------------------------------------------
        # [3.5선: AI 합동 언론 데스크 & 레드팀 크리틱 파이프라인]
        # ⚠️  editorial_result는 results 리스트에 추가하지 않는다.
        #     _build_briefing_report()가 results[0]~[4]를 하드코딩으로 참조하기 때문이다.
        #     편집장 결과물은 session_dir에 직접 저장하고 payload로 보관한다.
        # ---------------------------------------------
        print("\n📰 " * 10)
        print("[*] [Joint Editorial Board] 합동 언론 데스크 & 레드팀 파이프라인 가동...")
        print("📰 " * 10 + "\n")

        from agents.editorial_desk import run_editorial_board
        editorial_result = run_editorial_board(reporter_results, financial_data, session_dir)

        editorial_column = ""
        if editorial_result and editorial_result.success:
            editorial_column = editorial_result.payload.get("final_column", "")
            # 최종 칼럼을 세션 디렉토리에 별도 저장
            editorial_col_path = session_dir / "editorial_column.md"
            try:
                editorial_col_path.write_text(editorial_column, encoding="utf-8")
                print(f"[+] [3.5선 편집장] 최종 종합 칼럼 세션 아카이브 저장: {editorial_col_path.name}")
            except Exception as e:
                print(f"[!] [3.5선 편집장] 칼럼 세션 저장 실패: {e}")
        else:
            print("[!] [3.5선 편집장] 편집 데스크 실패 또는 건너뜀. 4선 Wiki 정상 진행합니다.")

        # ---------------------------------------------
        # [4선: Wiki 관리자 - 지식 융합 및 누적 컴파운딩]
        # ---------------------------------------------
        wiki_agent = WikiManager()
        wiki_res = wiki_agent.run_compounding(financial_data)  # 시그니처: financial_data 1개만 — 변경 없음
        results.append(wiki_res)
        print(f"[*] [4선 Wiki관리자] 위키 누적 합성 성공 여부: {wiki_res.success} | 갱신된 파일 수: {wiki_res.collected_count}개")
```

### `_build_briefing_report` 수정 — 편집장 사설 카드 추가

편집장 사설을 브리핑에 포함하기 위해 시그니처에 `editorial_column` 파라미터를 추가합니다.

**Before** (175줄):
```python
    def _build_briefing_report(self, results: List[AgentResult], total_elapsed: float, financial_data: Dict[str, Any]) -> str:
```

**After**:
```python
    def _build_briefing_report(self, results: List[AgentResult], total_elapsed: float, financial_data: Dict[str, Any], editorial_column: str = "") -> str:
```

그리고 `briefing` 문자열 조립부 마지막에 다음을 추가합니다 (`return briefing` 직전):

**Before** (실제 코드 236~238줄):
```python
        briefing = f"""🤖 *[Antigravity AI 비서 릴레이 파이프라인 브리핑]*
...
💡 *Tip*: ...
"""
        return briefing
```

**After** — briefing f-string 끝 Tip 줄 바로 뒤, `return briefing` 직전에 추가:
```python
        if editorial_column:
            # 사설이 길 경우 앞 500자만 미리보기로 텔레그램에 전송
            preview = editorial_column[:500].strip()
            if len(editorial_column) > 500:
                preview += "...\n_(전문은 세션 아카이브 `editorial_column.md` 참조)_"
            briefing += f"\n\n📰 *[편집장 종합 사설 미리보기]*\n{preview}\n"

        return briefing
```

`run_relay()` 내 `_build_briefing_report` 호출부도 함께 수정합니다.

**Before** (102줄):
```python
        briefing = self._build_briefing_report(results, total_elapsed, financial_data)
```

**After**:
```python
        briefing = self._build_briefing_report(results, total_elapsed, financial_data, editorial_column=editorial_column)
```

---

## 4. 변경: `telegram_bot.py` — 능동형 프로액티브 모니터링

### 4-1. 신규 상수 추가 (파일 상단 전역 설정 블록)

**Before** (33줄):
```python
# 중복 수집/분석 방지용 전역 락 플래그
is_updating = False
```

**After**:
```python
# 중복 수집/분석 방지용 전역 락 플래그
is_updating = False

# 프로액티브 모니터링 임계값 설정
PROACTIVE_BTC_CHANGE_THRESHOLD = 1.5     # BTC 24시간 변동률 절댓값 (%)
PROACTIVE_FX_CHANGE_THRESHOLD = 0.5      # 원/달러 환율 변동률 절댓값 (%)
PROACTIVE_STATE_PATH = ROOT_DIR / "_company" / "proactive_state.json"
PROACTIVE_COOLDOWN_HOURS = 4             # 동일 알림 재발송 최소 간격 (시간)
```

### 4-2. 신규 함수: `proactive_monitoring_loop()`

`scheduled_update_loop()` 정의(708줄) **바로 앞**에 삽입합니다.

```python
async def proactive_monitoring_loop():
    """
    [능동형 프로액티브 모니터링 루프]
    1시간 주기로 financial_data.json을 읽어 BTC 변동률 또는 환율 변동이
    임계값을 초과하면 텔레그램으로 능동적 경보를 발송합니다.
    - BTC 24h 변동률 ±1.5% 초과 시 경보
    - 이전 경보 시점 대비 환율 ±0.5% 초과 시 경보
    - 동일 경보는 최소 4시간 간격을 두고 재발송합니다.
    """
    # 봇 기동 직후 5분 대기 (초기 파이프라인 실행과 충돌 방지)
    await asyncio.sleep(300)

    while True:
        try:
            # ── 1. financial_data.json 읽기 ────────────────────────────────────
            if not FINANCIAL_DATA_PATH.exists():
                # DB 관리자가 아직 수집을 시작하지 않은 상태 — 조용히 건너뜀
                await asyncio.sleep(3600)
                continue

            with open(FINANCIAL_DATA_PATH, "r", encoding="utf-8") as f:
                fin_data = json.load(f)

            btc_change = fin_data.get("btc_krw_change", 0.0)
            exchange_rate = fin_data.get("exchange_rate", 0.0)
            btc_krw = fin_data.get("btc_krw", 0)

            # ── 2. 이전 경보 상태 로드 ──────────────────────────────────────────
            last_state = {}
            if PROACTIVE_STATE_PATH.exists():
                try:
                    with open(PROACTIVE_STATE_PATH, "r", encoding="utf-8") as f:
                        last_state = json.load(f)
                except Exception:
                    last_state = {}

            last_alerted_at_str = last_state.get("last_alerted_at", "")
            last_exchange_rate = last_state.get("last_exchange_rate", exchange_rate)

            # 쿨다운 체크
            in_cooldown = False
            if last_alerted_at_str:
                try:
                    last_alerted_at = datetime.fromisoformat(last_alerted_at_str)
                    elapsed_hours = (datetime.now() - last_alerted_at).total_seconds() / 3600
                    in_cooldown = elapsed_hours < PROACTIVE_COOLDOWN_HOURS
                except Exception:
                    in_cooldown = False

            # ── 3. 임계값 판단 ──────────────────────────────────────────────────
            btc_triggered = abs(btc_change) >= PROACTIVE_BTC_CHANGE_THRESHOLD
            fx_change_pct = abs((exchange_rate - last_exchange_rate) / last_exchange_rate * 100) if last_exchange_rate else 0
            fx_triggered = fx_change_pct >= PROACTIVE_FX_CHANGE_THRESHOLD

            if (btc_triggered or fx_triggered) and not in_cooldown and is_token_valid():
                print(f"[*] [ProactiveMonitor] 임계값 초과 감지 — BTC: {btc_change:+.2f}%, FX변동: {fx_change_pct:+.2f}%")

                # ── 4. Ollama로 짧은 경보 메시지 생성 ──────────────────────────
                alert_prompt = (
                    f"현재 금융 지표: BTC/KRW {btc_krw:,.0f}원 (24h {btc_change:+.2f}%), "
                    f"원/달러 {exchange_rate:,.2f}원\n\n"
                    "위 급격한 시장 변동에 대해 투자자에게 2~3문장의 간결한 한국어 경보 알림을 작성하십시오. "
                    "공포를 유발하지 말고 사실 중심으로 작성하십시오."
                )
                alert_body = await asyncio.to_thread(
                    call_ollama, alert_prompt,
                    "당신은 침착한 금융 데이터 모니터링 봇입니다."
                )
                if not alert_body:
                    # Ollama 장애 시 기본 메시지 사용
                    alert_body = (
                        f"BTC 24시간 변동: {btc_change:+.2f}%\n"
                        f"원/달러: {exchange_rate:,.2f}원 (이전 대비 {fx_change_pct:+.2f}%)"
                    )

                # ── 5. 텔레그램 경보 발송 (인라인 버튼 포함) ────────────────────
                keyboard = [[
                    InlineKeyboardButton("🔍 지금 전체 분석 가동", callback_data="proactive_run"),
                    InlineKeyboardButton("✅ 확인했어", callback_data="proactive_dismiss"),
                ]]
                reply_markup = InlineKeyboardMarkup(keyboard)

                from telegram import Bot
                bot = Bot(token=BOT_TOKEN)
                await bot.send_message(
                    chat_id=CHAT_ID,
                    text=(
                        f"⚡ *[프로액티브 시장 경보]*\n\n"
                        f"{alert_body}\n\n"
                        f"📊 BTC `{btc_krw:,.0f}원` ({btc_change:+.2f}%) | 환율 `{exchange_rate:,.2f}원`"
                    ),
                    reply_markup=reply_markup,
                    parse_mode="Markdown"
                )
                print(f"[+] [ProactiveMonitor] 텔레그램 경보 발송 완료.")

                # ── 6. 경보 상태 저장 ──────────────────────────────────────────
                new_state = {
                    "last_alerted_at": datetime.now().isoformat(timespec="minutes"),
                    "last_exchange_rate": exchange_rate,
                    "last_btc_change": btc_change,
                }
                try:
                    PROACTIVE_STATE_PATH.parent.mkdir(parents=True, exist_ok=True)
                    with open(PROACTIVE_STATE_PATH, "w", encoding="utf-8") as f:
                        json.dump(new_state, f, ensure_ascii=False, indent=2)
                except Exception as e:
                    print(f"[!] [ProactiveMonitor] 상태 파일 저장 실패: {e}")
            else:
                print(f"[*] [ProactiveMonitor] 임계값 미달 또는 쿨다운 중. 다음 점검: 1시간 후.")

        except Exception as e:
            print(f"[!] [ProactiveMonitor] 모니터링 루프 예외 발생: {e}")

        await asyncio.sleep(3600)  # 1시간 대기
```

### 4-3. `button_callback_handler` — 프로액티브 콜백 분기 추가

**Before** (`button_callback_handler` 함수 내, 368줄):
```python
    data = query.data
    action, wiki_name = data.split("_", 1)
    
    draft_path = ROOT_DIR / "_company" / "approvals" / "pending" / f"{wiki_name}_draft.md"
    official_path = Path(WIKI_DIR) / f"{wiki_name}.md"
    
    await log_chat_message(sender="user", message_text=f"[Callback Click] {data}", session_id=str(query.message.chat_id))

    if action == "approve":
```

**After** — `await log_chat_message(...)` 바로 뒤에 프로액티브 분기를 추가:
```python
    data = query.data
    action, wiki_name = data.split("_", 1)

    draft_path = ROOT_DIR / "_company" / "approvals" / "pending" / f"{wiki_name}_draft.md"
    official_path = Path(WIKI_DIR) / f"{wiki_name}.md"

    await log_chat_message(sender="user", message_text=f"[Callback Click] {data}", session_id=str(query.message.chat_id))

    # ── [R11 추가] 프로액티브 모니터링 경보 콜백 처리 ──────────────────────────
    if action == "proactive":
        sub_action = wiki_name  # "run" 또는 "dismiss"
        global is_updating

        if sub_action == "dismiss":
            msg = await query.edit_message_text(
                text="✅ *확인 완료.* 시장 모니터링을 계속합니다.",
                parse_mode="Markdown"
            )
            await log_chat_message(sender="bot", message_text=msg.text or "", session_id=str(query.message.chat_id))
            return

        elif sub_action == "run":
            if is_updating:
                msg = await query.edit_message_text(
                    text="🔄 다른 분석이 이미 진행 중입니다. 완료 후 결과를 보내드릴게요."
                )
                await log_chat_message(sender="bot", message_text=msg.text or "", session_id=str(query.message.chat_id))
                return

            msg = await query.edit_message_text(
                text="⚙️ *전체 에이전트 릴레이 파이프라인을 가동합니다...* (약 1~3분 소요)",
                parse_mode="Markdown"
            )
            is_updating = True
            try:
                agent = ChiefAgent()
                briefing = await asyncio.to_thread(agent.run_relay)
                await query.message.reply_text(briefing, parse_mode="Markdown")
                await log_chat_message(sender="bot", message_text=briefing or "", session_id=str(query.message.chat_id))
            except Exception as e:
                await query.message.reply_text(f"❌ 릴레이 실행 중 오류: `{e}`", parse_mode="Markdown")
            finally:
                is_updating = False
            return
        return  # 알 수 없는 sub_action 안전 처리

    if action == "approve":
```

### 4-4. `main()` — 버그 수정 및 프로액티브 루프 등록

**Before** (752~754줄):
```python
    async def post_init(application: Application):
        asyncio.create_task(scheduled_update_loop())
        print("[+] 12시간 백그라운드 자동 스케줄러 태스크 등록 및 백그라운드 스레드 기동 완료.")
```

**After**:
```python
    # [R11 버그 수정] init_chat_db()가 main()에서 누락되어 있었음.
    # 최초 핸들러가 log_chat_message()를 호출하기 전에 반드시 DB를 초기화해야 함.
    init_chat_db()

    async def post_init(application: Application):
        asyncio.create_task(scheduled_update_loop())
        asyncio.create_task(proactive_monitoring_loop())  # [R11 신설] 능동형 모니터링 루프 등록
        print("[+] 12시간 자동 스케줄러 + 능동형 프로액티브 모니터링 루프 등록 완료.")
```

### 4-5. `--send-briefing` 진입점 — `init_chat_db` 누락 버그 동시 수정

**Before** (776~780줄):
```python
if __name__ == "__main__":
    if len(sys.argv) > 1 and sys.argv[1] == "--send-briefing":
        asyncio.run(send_briefing_notification())
    else:
        main()
```

**After**:
```python
if __name__ == "__main__":
    if len(sys.argv) > 1 and sys.argv[1] == "--send-briefing":
        init_chat_db()  # [R11 버그 수정] send_briefing_notification()이 log_chat_message()를 호출하므로 선행 초기화 필수
        asyncio.run(send_briefing_notification())
    else:
        main()
```

---

## 5. Known Unknowns

**① `call_ollama` 타임아웃 미조정 리스크**
`call_ollama()`는 `timeout=90`이 하드코딩되어 있습니다. 레드팀 파이프라인 5회 순차 호출 기준 최대 450초가 소요될 수 있습니다. `asyncio.to_thread`로 이벤트 루프는 보호되지만, `/update` 커맨드의 `status_msg`는 파이프라인이 끝날 때까지 갱신되지 않아 사용자가 봇이 멈춘 것으로 착각할 수 있습니다. 해결은 다음 버전에서 `update_command` 내 중간 상태 메시지 갱신 코루틴을 추가하거나, `ollama_client.py`의 timeout 파라미터를 외부에서 주입 가능하게 변경해야 합니다.

**② 리포터 `files_created`가 비어 있는 케이스**
리포터가 DeepSearch API와 RSS 피드 모두 실패하면 `AgentResult.files_created`가 빈 리스트입니다. `editorial_desk.py`는 이 경우 해당 기자의 원고를 공백으로 처리하고 계속 진행합니다. 단, 3개 기자 전체가 실패하면 `None`을 반환하여 데스크를 중단합니다. `chief_agent.py`에서 `None` 체크는 After 코드에 명시되어 있습니다.

**③ 프로액티브 경보와 수동 `/update`의 `is_updating` 경합**
`proactive_run` 콜백이 `is_updating = True`로 설정하는 중에 사용자가 `/update`를 입력하면, `update_command`의 `is_updating` 체크가 이를 차단합니다. 이는 의도된 동작입니다. 단, 프로액티브 콜백에서 `run_relay()` 실행 중 예외가 발생할 경우 `finally` 블록이 `is_updating = False`를 보장합니다.

**④ 프로액티브 `Bot()` 인스턴스 직접 생성**
`proactive_monitoring_loop()`와 `proactive_run` 콜백에서 `Bot(token=BOT_TOKEN)`을 직접 생성합니다. PTB(python-telegram-bot) v20의 `Application` 인스턴스와 별도로 동작하므로 rate limit 계산이 분리됩니다. 텔레그램의 봇당 초당 30메시지 제한 내에서는 문제가 없으나, 향후 고빈도 알림이 추가될 경우 `Application.bot`을 공유하는 방식으로 리팩토링이 필요합니다.

---

## 6. Verification Plan

### 기능 검증 체크리스트

**[A] 버그 수정 검증**
- `python telegram_bot.py` 실행 시 콘솔에 `[+] 텔레그램 로컬 SQLite 대화 데이터베이스 초기화 완료 (WAL & Lock).` 출력 확인
- `python telegram_bot.py --send-briefing` 실행 시 `OperationalError: no such table` 없이 정상 종료 확인

**[B] 편집 데스크 파이프라인 검증**
- `/update` 구동 후 콘솔 로그에 다음 순서가 에러 없이 출력되는지 확인:
  1. `[*] [EditorialDesk] 리포터 원고 파일 수집 중...`
  2. `[*] [EditorialDesk] 레드팀 교차 감사 개시...`
  3. `[+] [EditorialDesk] 레드팀 감사 지침 발부 완료.`
  4. `[*] [EditorialDesk] KoreaReporter 수정 원고 생성 중...` (3회)
  5. `[*] [EditorialDesk] 편집장 최종 종합 사설 작성 중...`
  6. `[+] [EditorialDesk] 전체 편집 데스크 세션 완료`
- `/update` 완료 후 해당 세션 디렉토리(`_company/sessions/YYYY-MM-DDTHH-MM/`)에 `editorial_minutes.md`와 `editorial_column.md`가 생성되었는지 파일 시스템 확인
- 텔레그램으로 수신된 브리핑 메시지에 `📰 [편집장 종합 사설 미리보기]` 카드가 포함되어 있는지 수동 확인

**[C] 프로액티브 모니터링 검증**
- `PROACTIVE_BTC_CHANGE_THRESHOLD`를 임시로 `0.01`로 낮춘 뒤 봇 재기동하여 5분 후 텔레그램에 `⚡ [프로액티브 시장 경보]` 메시지가 수신되는지 확인
- 경보 메시지의 `[🔍 지금 전체 분석 가동]` 버튼 클릭 → 릴레이 파이프라인 정상 가동 확인
- `[✅ 확인했어]` 버튼 클릭 → 메시지가 `확인 완료.` 로 수정되는지 확인
- `_company/proactive_state.json`이 경보 발송 후 생성되는지 확인
- 쿨다운 4시간 내 재경보가 발송되지 않는지 확인 (임계값을 낮게 유지한 채 2회 연속 1시간 대기)

**[D] 인덱스 오염 방지 검증**
- `chief_agent.py`의 `_build_briefing_report()` 호출부에서 `results` 길이가 여전히 5(`results[0]`~`results[4]`)임을 assert로 확인하거나, 브리핑 메시지의 `5선 Wiki관리` 카드가 정상적으로 출력되는지 목시 확인
