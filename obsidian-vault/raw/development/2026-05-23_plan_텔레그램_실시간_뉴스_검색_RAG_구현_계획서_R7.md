# 텔레그램 비서 에이전트 실시간 웹 뉴스 검색 (RAG) 기능 보완 구현 계획서 (R7)

사용자가 최신 이슈(예: "삼성전자 이번 주 이슈 정리해줘")를 물어봤을 때, 봇이 관련 자료를 요청하는 수동적 방식 대신 **실시간으로 구글 뉴스 RSS 피드를 스크래핑 및 검색**하여 요약 보고서를 즉시 작성 및 브리핑하는 지능형 실시간 비서 기능(RAG)의 구현 계획서입니다.

---

## 1. User Review Required

> [!IMPORTANT]
> **사용자 자료 제출 불필요 (100% 자동화)**
> 더 이상 사용자에게 관련 기사나 파일을 달라고 요청하지 않습니다. 봇이 내부적으로 구글 뉴스 RSS 피드 API 없이도 사용 가능한 주소를 스캔하여 실시간으로 요약 브리핑합니다.

> [!TIP]
> **지능형 뉴스 판별 및 키워드 추출**
> Ollama 모델(`gemma4:e4b`)을 활용하여 사용자의 자연어 발화가 실시간 뉴스를 원하는지 자동 판정하고, 최적의 검색용 핵심 키워드를 실시간 추출하여 RAG 검색을 구동합니다.

---

## 2. Proposed Changes

### [Component 1] 텔레그램 봇 데몬 및 RAG 모듈

#### [MODIFY] [telegram_bot.py](file:///c:/Users/jmj/Desktop/안티그래비티/new/telegram_bot.py)

* **주요 변경 사항**:
  1. `fetch_realtime_news(query, limit=5)`: 구글 뉴스 RSS 피드(`https://news.google.com/rss/search?q={query}&hl=ko&gl=KR&ceid=KR:ko`)를 스크래핑하여 최신 뉴스의 제목, 링크, 배포처, 요약을 파싱하는 헬퍼 함수 추가.
  2. `analyze_news_request(text)`: 사용자의 발화가 뉴스 검색 요구인지 분석하고 검색어 키워드를 추출하여 리턴하는 AI 헬퍼 추가.
  3. `text_message_handler`:
     * 사용자의 발화가 일반 잡담인지, 아니면 뉴스 검색 요구인지 `analyze_news_request`를 통해 분석.
     * 뉴스 검색이 요구될 경우 `💬 비서가 실시간 뉴스를 수집하고 분석하는 중...` 시각 피드백 제공.
     * 수집된 실시간 뉴스 컨텍스트를 Ollama 프롬프트에 주입하여, 다정하고 친근한 비서 톤의 한국어 구어체 실시간 이슈 요약 보고서를 동적으로 생성 후 회신.

---

## 3. 코드 사전 감사 및 Before / After 변경 계획

### [감사 대상 함수: `text_message_handler` 및 신설 헬퍼들]

#### [Before] `telegram_bot.py` (라인 486-543)
```python
async def text_message_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """사용자가 슬래시 없이 한글로 텍스트 메시지를 보냈을 때 지능적으로 판단하여 위키 전송 혹은 LLM 기반 지능형 일상대화 수행"""
    text = update.message.text.strip() if update.message.text else ""
    
    # [R5 해결 - 이중 로깅 차단 방안 A 적용]
    # text_message_handler 내부의 로깅을 전면 제거하여 하위 커맨드 핸들러의 1회 로깅과 충돌/중복을 완전히 예방합니다.

    # 1. 키워드 기반 위키 요약/제어 분기 매칭
    if any(kw in text for kw in ["업데이트", "가져와", "수집", "분석", "동기화", "뉴스"]):
        await update_command(update, context)
    elif "비트코인" in text or "bitcoin" in text.lower():
        await bitcoin_command(update, context)
    elif "세계경제" in text or "fed" in text.lower() or "연준" in text:
        await fed_command(update, context)
    elif "국내경제" in text or "korea" in text.lower() or "한국경제" in text:
        await korea_command(update, context)
    elif "로그" in text or "log" in text.lower() or "기록" in text:
        await log_command(update, context)
    elif text.startswith("/") or any(text.startswith(kw) for kw in ["도움말", "명령어", "메뉴", "시작"]):
        await start_command(update, context)
    else:
        # [R6 지능형 일상대화 피처 연동]
        # 사용자 질문(발화) 로깅
        await log_chat_message(sender="user", message_text=text, session_id=str(update.message.chat_id))
        
        # 봇이 입력 중(typing...) 상태를 화면에 실시간 노출하여 UX 편의 극대화
        try:
            await context.bot.send_chat_action(chat_id=update.message.chat_id, action="typing")
        except Exception as chat_action_err:
            print(f"[!] Chat Action 발송 실패: {chat_action_err}")
            
        system_prompt = (
            "당신은 로컬 Obsidian LLM Wiki를 보좌하는 지능형 비서 에이전트 'Antigravity 비서'입니다.\n"
            "사용자의 물음에 다정하고, 프로페셔널하며, 지적이고 예의바르게 한국어로 답변해주세요.\n"
            "너무 딱딱한 로봇 같은 서술형식 대신 자연스럽고 친근한 비서 톤의 구어체로 짧고 명료하게 소통하십시오."
        )
        
        # 비동기 이벤트 루프를 블로킹하지 않고 별도 동기 스레드 풀에 로컬 Ollama 호출 위임
        ai_response = await asyncio.to_thread(call_ollama, text, system_prompt)
        
        if not ai_response:
            # Ollama 오프라인/연결 실패 시 세련된 가이드라인 폴백 제공
            ...
```

#### [After] `telegram_bot.py` (구글 뉴스 RSS 파싱 헬퍼 + 지능형 RAG 분기 도입)
```python
import urllib.parse
import xml.etree.ElementTree as ET
from bs4 import BeautifulSoup
from agents.shared.ollama_client import call_ollama_json

def fetch_realtime_news(query: str, limit: int = 5) -> list:
    """구글 뉴스 RSS 피드를 활용하여 실시간 뉴스를 안전하게 스크래핑합니다."""
    encoded_query = urllib.parse.quote(query)
    rss_url = f"https://news.google.com/rss/search?q={encoded_query}&hl=ko&gl=KR&ceid=KR:ko"
    headers = {
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
    }
    articles = []
    try:
        # 동기 HTTP 요청이므로 호출 시 asyncio.to_thread로 감싸 실행
        response = requests.get(rss_url, headers=headers, timeout=10)
        if response.status_code == 200:
            root = ET.fromstring(response.content)
            items = root.findall(".//item")
            for item in items[:limit]:
                title = item.find("title").text if item.find("title") is not None else ""
                link = item.find("link").text if item.find("link") is not None else ""
                pub_date = item.find("pubDate").text if item.find("pubDate") is not None else ""
                description = item.find("description").text if item.find("description") is not None else ""
                
                soup_desc = BeautifulSoup(description, "html.parser")
                clean_desc = soup_desc.get_text()
                
                # 언론사 정보 파싱 시도 (제목의 ' - 언론사' 포맷 활용)
                publisher = "구글 뉴스"
                if " - " in title:
                    parts = title.rsplit(" - ", 1)
                    title = parts[0]
                    publisher = parts[1]
                
                articles.append({
                    "title": title,
                    "url": link,
                    "pub_date": pub_date,
                    "description": clean_desc,
                    "publisher": publisher
                })
    except Exception as e:
        print(f"[!] 실시간 뉴스 RSS 수집 장애 발생: {e}")
    return articles

async def analyze_news_request(text: str) -> (bool, str):
    """
    사용자의 입력이 실시간 뉴스/이슈 검색 및 요약 요청인지 AI를 통해 고도로 분석하고,
    검색 엔진에 전달할 핵심 키워드를 정확히 추출합니다.
    """
    keywords = ["뉴스", "이슈", "소식", "최근", "요약", "정리", "근황", "보여줘", "알려줘", "보고해"]
    # 1. 1차 빠른 매칭 검사
    if not any(kw in text for kw in keywords):
        return False, ""
        
    # 2. 2차 Ollama 기반 지능형 스키마 분석
    prompt = (
        f"사용자의 자연어 질문: '{text}'\n\n"
        "위 질문이 실시간 인터넷 뉴스, 기업 근황, 거시경제 이슈 등에 대한 웹 검색 요약을 요청하는 내용인지 판별해 주세요.\n"
        "만약 검색이 필요하다면, 구글 뉴스 RSS 피드에 전달할 명사 위주의 가장 핵심적인 검색 키워드(예: '삼성전자', '비트코인 규제', '금리 인하' 등)를 단 한 개만 추출하십시오.\n"
        "반드시 다음 JSON 형식에 정확히 맞춰서만 답변해야 하며, 사족이나 보충 설명은 절대로 출력하지 마십시오:\n"
        '{"is_news_request": true, "search_keyword": "추출한키워드"}'
    )
    
    def _call():
        return call_ollama_json(prompt, "당신은 한국어 핵심 키워드 및 의도 판정기입니다. 사족 없이 오직 JSON만 반환합니다.")

    try:
        res = await asyncio.to_thread(_call)
        if res and isinstance(res, dict):
            return res.get("is_news_request", False), res.get("search_keyword", "")
    except Exception as e:
        print(f"[!] 올라마 뉴스 의도 판단 실패: {e}")
    return False, ""

async def text_message_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """사용자가 슬래시 없이 한글로 텍스트 메시지를 보냈을 때 지능적으로 판단하여 위키 전송 혹은 LLM 기반 지능형 일상대화 수행"""
    text = update.message.text.strip() if update.message.text else ""
    
    # 1. 키워드 기반 위키 요약/제어 분기 매칭
    # (업데이트 요청 시 12시간 데몬 릴레이가 가동되고, 단독 키워드는 로컬 위키 요약을 즉시 조회합니다.)
    if any(kw in text for kw in ["업데이트", "가져와", "동기화"]):
        await update_command(update, context)
        return
    elif "비트코인" in text or "bitcoin" in text.lower():
        await bitcoin_command(update, context)
        return
    elif "세계경제" in text or "fed" in text.lower() or "연준" in text:
        await fed_command(update, context)
        return
    elif "국내경제" in text or "korea" in text.lower() or "한국경제" in text:
        await korea_command(update, context)
        return
    elif "로그" in text or "log" in text.lower() or "기록" in text:
        await log_command(update, context)
        return
    elif text.startswith("/") or any(text.startswith(kw) for kw in ["도움말", "명령어", "메뉴", "시작"]):
        await start_command(update, context)
        return

    # 2. [R7 지능형 실시간 뉴스 RAG 검색 기능] 도입
    # 사용자의 질문이 특정 이슈/실시간 뉴스를 요구하는지 지능적으로 확인
    is_news, search_kw = await analyze_news_request(text)
    
    if is_news and search_kw:
        # 사용자 질문(발화) 로깅
        await log_chat_message(sender="user", message_text=text, session_id=str(update.message.chat_id))
        
        # 봇이 검색 및 분석 중 상태를 텔레그램 화면에 실시간 노출
        try:
            await context.bot.send_chat_action(chat_id=update.message.chat_id, action="typing")
            # "실시간 뉴스를 검색하고 정리 중입니다" 선행 알림
            temp_msg = await update.message.reply_text(
                f"🔍 *[실시간 웹 검색 RAG 가동]*\n사용자님의 요청을 위해 인터넷에서 `{search_kw}` 관련 최신 뉴스를 수집하고 있습니다. 잠시만 기다려 주세요... 💬",
                parse_mode="Markdown"
            )
            await context.bot.send_chat_action(chat_id=update.message.chat_id, action="typing")
        except Exception as chat_action_err:
            print(f"[!] RAG 안내 메시지 발송 실패: {chat_action_err}")
            temp_msg = None

        # 구글 뉴스 RSS 실시간 검색 실행
        def _search():
            return fetch_realtime_news(search_kw, limit=5)
            
        news_list = await asyncio.to_thread(_search)
        
        if not news_list:
            if temp_msg:
                await temp_msg.delete()
            err_msg = await update.message.reply_text(
                f"⚠️ *[검색 결과 없음]*\n인터넷에서 `{search_kw}`와(과) 관련된 실시간 뉴스를 찾지 못했습니다. 키워드를 변경해 보시거나 잠시 후 다시 시도해 주세요.",
                parse_mode="Markdown"
            )
            await log_chat_message(sender="bot", message_text=err_msg.text or "", session_id=str(update.message.chat_id))
            return

        # RAG 콘텍스트 빌드
        context_str = ""
        for idx, news in enumerate(news_list):
            context_str += f"[{idx+1}] 제목: {news['title']}\n    출처: {news['publisher']} ({news['pub_date']})\n    내용: {news['description']}\n\n"

        # 올라마에게 RAG 요약 보고서 지시 프롬프트 작성
        system_prompt = (
            "당신은 실시간 인터넷 뉴스 검색 결과를 바탕으로 요약 보고서를 친절히 브리핑하는 'Antigravity 지능형 비서'입니다.\n"
            "사용자의 질문에 부합하도록, 아래 수집된 실시간 뉴스를 바탕으로 핵심 이슈와 중요 흐름을 다정하고 조리 있게 한국어 구어체로 설명해 주십시오.\n"
            "너무 딱딱한 뉴스 나열식이 아니라, 친구 혹은 믿음직한 개인 비서처럼 세련되고 구조적인 레이아웃(이모지 활용)으로 설명하십시오.\n"
            "답변 끝에 수집된 원본 뉴스의 출처(제목 및 언론사)를 깔끔하게 크레딧 형태로 첨부해 신뢰도를 극대화하십시오."
        )
        
        rag_prompt = (
            f"사용자 질문: '{text}'\n\n"
            f"[실시간 검색된 `{search_kw}` 관련 뉴스 기사 컨텍스트]\n"
            f"{context_str}\n"
            "위 컨텍스트를 활용하여 사용자가 만족할 수 있는 세련되고 명쾌한 요약 보고를 작성해 주십시오."
        )

        # 요약 생성 위임
        ai_response = await asyncio.to_thread(call_ollama, rag_prompt, system_prompt)
        
        if temp_msg:
            await temp_msg.delete()

        if not ai_response:
            # Fallback
            fallback_msg = (
                f"🔌 *[AI 분석 지연 안내]*\n"
                f"`{search_kw}` 관련 뉴스는 성공적으로 검색되었으나 로컬 AI 요약 엔진이 일시적인 대기 상태입니다.\n\n"
                f"수집된 최신 뉴스 제목 목록입니다:\n" + 
                "\n".join([f"• {n['title']} ({n['publisher']})" for n in news_list])
            )
            msg = await update.message.reply_text(fallback_msg, parse_mode="Markdown")
            await log_chat_message(sender="bot", message_text=msg.text or "", session_id=str(update.message.chat_id))
        else:
            msg = await update.message.reply_text(ai_response, parse_mode="Markdown")
            await log_chat_message(sender="bot", message_text=msg.text or "", session_id=str(update.message.chat_id))
            
        return

    # 3. 만약 뉴스 검색이 아니라면 기존 다정한 일상 잡담 대화 (R6 흐름) 진행
    # (이하 기존 일상 잡담 로직 동일 유지)
    ...
```

---

## 4. Known Unknowns (자기완성도 선언 배제)

> [!WARNING]
> **검색 기사 수집 한계**
> 구글 뉴스 RSS는 API 키 없이 구동되므로 일시적인 네트워크 순단이나 속도 저하에 영향을 받을 수 있습니다. 만약 구글 피드 수집이 지연되거나 반환이 안 될 시, 안내 메시지와 함께 기존 로컬 캐시/위키로 안전하게 폴백하도록 예외 처리 코드를 강화합니다.
> 
> **AI 요약 모델(gemma4)의 요약 품질**
> 로컬 AI 특성상 프롬프트에 제공된 뉴스 컨텍스트의 정보가 너무 방대하거나 중첩될 시, 요약의 정확도가 모델의 성능에 제한을 받을 수 있습니다. 이를 막기 위해 최신 핵심 5건의 간략 피드로 기사 분량을 최적화하여 든든하게 요약하도록 유도합니다.

---

## 5. Verification Plan

### Automated / Integration Tests
- 봇 재시동 후 텔레그램을 통해 `"이번주 삼성전자 이슈 정리해줘"` 또는 `"테슬라 소식 요약 보고해"`와 같이 뉴스 키워드를 던져 RAG가 작동하는지 브라우저 및 메신저 콘솔에서 확인합니다.
- 데이터베이스 쿼리를 조회하여 사용자의 RAG 유도 발화 및 봇의 실시간 AI RAG 응답이 `telegram_chat_logs` 테이블에 중복 없이 정확히 1회 적재되었는지 검증합니다.

### Manual Verification
- 사용자에게 배포 완료를 인지시키고 텔레그램 메신저에서 직접 질문하여 원하는 삼성전자 뉴스가 아름답게 요약 보고되는지 최종 점검을 요청합니다.
