# 텔레그램 능동형 프로액티브 모니터링 & AI 합동 언론 데스크 파이프라인 구현 계획서 (R11)

기존 계획서의 치명적인 TypeError(시그니처 불일치), IndexError(results 리스트 크래시), Before 코드의 오류를 코드 레벨 정밀 감사를 통해 완전히 교정하고, 1시간 주기의 임계값 프로액티브 감시 모듈 및 3단계 에디토리얼 데스크(레드팀 감사 ➡️ 기자단 수정 ➡️ 편집장 종합 편찬)를 무결하게 구현하기 위한 최종 R11 구현 계획서입니다.

---

## 1. User Review Required & Bug Fixes

> [!IMPORTANT]
> **R10 설계 결함 3건 완벽 해결**
> 1. `WikiManager.run_compounding()` 시그니처가 인자 1개(`financial_data`)만 취하므로, 2개 인자를 주입하려던 TypeError를 원천 예방합니다.
> 2. `_build_briefing_report()`가 `results[0]`~`results[4]`를 하드코딩 참조하므로, `results` 인덱스 순서를 무너뜨리지 않기 위해 `editorial_result`를 리스트 외부로 격리 적재하고 `session_dir`에 별도 저장하도록 보호합니다.
> 3. `telegram_bot.py` 기동 시 `init_chat_db()` 누락으로 발생하던 `OperationalError` 기존 버그를 전격 수정합니다.

> [!TIP]
> **자율형 프로액티브 모니터링 가동**
> 1시간 주기로 지표 변동성(BTC ±1.5% 또는 환율 ±0.5% 변동)을 은밀히 체크하고, 임계값 초과 시 AI가 다정하게 경보 카드를 전송합니다. 하단에 `[🔍 지금 전체 분석 가동]`, `[✅ 확인했어]` 인라인 버튼을 부착하여 4시간 쿨다운 필터와 함께 실시간 기동합니다.

---

## 2. Proposed Changes & Code Pre-Audit

### [NEW] [agents/editorial_desk.py](file:///c:/Users/jmj/Desktop/안티그래비티/new/agents/editorial_desk.py)
* **레드팀 ➡️ 기자 수정 ➡️ 편집장 사설 편찬** 프로세스를 담은 무결한 동기식 헬퍼 모듈 구축.
* 기사 취재 원고를 메모리가 아닌 `AgentResult.files_created` 파일 경로에서 직접 실물 로드하여 팩트 기반의 RAG 컨텍스트 수립.
* 치열했던 상호 회의 대화록은 `editorial_minutes.md`로, 최종 통합 사설은 `editorial_column.md`로 각각 격리 세션 디렉토리에 백업 영속화.

### [MODIFY] [agents/chief_agent.py](file:///c:/Users/jmj/Desktop/안티그래비티/new/agents/chief_agent.py)
* `run_relay()`의 중간 파이프라인에서 단순 수집이 끝나면 즉시 신설된 `editorial_desk.py`에 오케스트레이션을 위임하여 **레드팀 감사 ➡️ 기자단 수정 ➡️ 편집장 최종 융합**이라는 3단계 데스크 세션을 강제 개시합니다.
* 최종적으로 편집장이 승인 완료한 명품 칼럼 원고만을 공식 위키에 수동 승인 대기하도록 `WikiManager` 대기실에 격리 전달합니다.
* 텔레그램 요약 브리핑 시 **"🤖 [AI 합동 뉴스룸 치열한 상호 토론 중계]"** 카드와 피어 리뷰 키워드를 텔레그램으로 함께 전송하여 투명성을 실시간 중계합니다.

### [MODIFY] [telegram_bot.py](file:///c:/Users/jmj/Desktop/안티그래비티/new/telegram_bot.py)
* **신규 상수 선언**: `PROACTIVE_BTC_CHANGE_THRESHOLD`, `PROACTIVE_FX_CHANGE_THRESHOLD`, `PROACTIVE_COOLDOWN_HOURS` 등 설정.
* **프로액티브 감시 루프 (`proactive_monitoring_loop`)**: 1시간 간격 작동 루프 신설 및 `main()`의 `asyncio.create_task()`에 정식 태스크 등록.
* **콜백 핸들러 개편**: `button_callback_handler` 내 `proactive_run` 및 `proactive_dismiss` 콜백 분기 무결 이식.
* **기존 버그 수정**: `main()` 및 `--send-briefing` 분기에 `init_chat_db()` 호출 추가.

---

## 3. Verification Plan

### Automated & Manual Verification
1. `python telegram_bot.py` 실행 시 콘솔에 SQLite DB 초기화 확인.
2. `PROACTIVE_BTC_CHANGE_THRESHOLD`를 `0.01`로 낮춘 뒤 봇을 재기동하여 5분 후 프로액티브 알림 송출 및 인라인 `지금 전체 분석 가동` 버튼을 통한 릴레이 파이프라인 연계 및 편집장 칼럼 미리보기 텔레그램 수신 확인.
3. 세션 디렉토리 내 `editorial_minutes.md` 및 `editorial_column.md` 실물 파일 생성 대조 검증.
